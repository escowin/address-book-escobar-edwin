<%# ::bug:: form_with model can either be:
- person#show | model: [ @person, @person.phone_numbers.build ] | create submit at person & address views
- phone_numbers#edit |  model: [ @phone_number.person, @phone_number], local: true | update submit at phone number view, but breaks person view %>
<%= form_with model: [ @person, @person.phone_numbers.build ] do |form| %>
    <article class="container">
        <%= form.label :phone, "Phone number (required)" %>
        <%= form.text_field :phone, class:"form-control", placeholder: "###-###-####" %>
    </article>

    <article class="container">
        <%= form.label :comment %>
        <%= form.text_field :comment, class:"form-control" %>
    </article>

    <article class="py-3 px-2">
        <%= form.submit class:"btn btn-dark w-100 rounded-5" %>
    </article>
<% end %>