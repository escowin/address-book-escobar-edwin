<%# ::bug:: form_with model can either be:
- person#show | model: [ @person, @person.emails.build ] | create submit at person & address views
- emails#edit |  model: [ @email.person, @email], local: true | update submit at email view, but breaks person view %>
<%= form_with model: [ @person, @person.emails.build ] do |form| %>
    <article class="container">
        <%= form.label :email, "Email (required)" %>
        <%= form.text_field :email, class:"form-control", placeholder: "user@email.com" %>
        <% @person.errors.full_messages_for(:email).each do |message| %>
            <div><%= message %></div>
        <% end %>
    </article>

    <article class="container">
        <%= form.label :comment %>
        <%= form.text_area :comment, class:"form-control" %>
    </article>

    <article class="py-3 px-2">
        <%= form.submit class:"btn btn-dark w-100 rounded-5" %>
    </article>
<% end %>